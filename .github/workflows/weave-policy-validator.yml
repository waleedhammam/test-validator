name: Weaveworks
on:
  push:
    branches: [ master ]
jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: waleedhammam/weave-action@v1
        with:
          path: ./
          policies-path: ./policies
          remediate: true # enable auto remediation
